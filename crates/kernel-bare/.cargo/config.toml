# Temporarily disabled due to toolchain path conflicts
# [unstable]
# build-std = ["core", "alloc"]
# build-std-features = ["compiler-builtins-mem"]

[build]
rustdocflags = ["-Zunstable-options"]

# Per-target compilation flags
[target.x86_64-unknown-none]
rustflags = [
    "-C", "link-arg=-T",
    "-C", "link-arg=linker.ld",
    "-C", "relocation-model=static",
    "-C", "link-arg=--nmagic",
]
